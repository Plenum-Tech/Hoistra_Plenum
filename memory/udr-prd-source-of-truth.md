---
name: udr-prd-source-of-truth
description: The PRD/ontology reference docs that are the source of truth for UDR/PropMg implementation
metadata:
  type: reference
---

As of 2026-06-18 the authoritative requirements for the AI-Native Property Management / UDR product live in `docs/reference/` (extracted from .docx the user added at D:\Plenum\):
- `UDR_PRD_v1.1_UserFlows_AcceptanceCriteria.txt` — **primary for acceptance criteria / QA** (Features 1 SDO, 2 Saved Spaces, 3 Pinned Runs, 4 Saved UDR Script, Activity Log, 7 UDR Mechanics; each user flow has Func/Metric AC).
- `1_UDR_forPropMg_Features.txt` — **primary for functional scope** (latest accepted; embeds PRD tables).
- `1_PropMg_Features.txt` — FM ontology + UDR mechanics rationale (track-changes narrative).
- `UDR_Activity_Log_Sample.html` — **canonical Activity Log UI/UX** (3 collapsible sections: Activity Summary, Processing Log [CoT/CoA], Human-in-the-loop; + Refinements; FAQ `?` popups; action cards ≤4 options).

The consolidated plan is `docs/UDR_CONSOLIDATED_IMPLEMENTATION_PLAN.md` — read it before any UDR/PropMg build. Phase-I artifacts built 2026-06-18 (decisions: extend plenum_cafm, in-Postgres relationship graph, latest-agreed numeric ACs): Phase I-A schema = `cafm-connector-service/migrations/udr_phase1_schema.sql` (idempotent; 32 new canonical + UDR-control + activity-log/saved-space/pinned-run tables, guarded FKs/ALTERs). Phase I-B RAG foundation = `svc-ai-schema-mapper/ontology/` (canonical_entities.json [single source of truth], synonyms.json, relationships.json, hierarchy.json, mapping_dictionaries/, build_rag_chunks.py → rag/chunks/*.md + manifest.json). Runtime/apply tooling now also built: `ontology/seed_udr_catalog.py` (→ idempotent `seed_udr_catalog.sql` for udr_canonical_table/column + udr_synonym), `ontology/embed_chunks.py` (dry-run default; `--apply` embeds chunks into udr_ontology_chunk), and `src/ontology_loader.py` (runtime accessor: resolve_table/resolve_column with deterministic→RAG-synonym tiers, relationships(), hierarchy(); `python src/ontology_loader.py` self-tests). Mapper wiring (2026-06-18, additive + flag-gated): `src/graph/nodes/ontology_mapping.py` adapter + **Strategy O** in `deterministic_mapper.py` (after registry, before Haiku; enable via env `UDR_ONTOLOGY_ENABLED=true`, default off → unchanged); `src/graph/nodes/udr_tests.py` (pure `run_test_1`/`run_test_2` + `test_1_node`/`test_2_node`, unit-tested in `tests/test_udr_phase1.py`). Runbook: `docs/UDR_PHASE1_WIRING.md`. Test 2 IS now wired into `migration_graph.py` (flag-gated: `UDR_TESTS_ENABLED=true` → verify_hierarchy→test_2_node→output; default off = unchanged graph; test_2_node not in _STEP_NODES so no pause; it records `state['udr_test_results']['test_2']`, additive — not yet a hard interrupt gate). STILL NOT DONE (do NOT run against prod without confirmation): applying SQL+seed+embeddings to the live Azure DB; turning Test 2 into a hard review gate (interrupt + 3 resolution options + UI); wiring Test 1 once Layer-2 chunk data is threaded in; end-to-end pipeline validation with the stack running. relationships.json is read at runtime, not seeded globally (udr_relationship is per-run, org_id NOT NULL). It maps PRD features to existing code (SDO=svc-deepagents, UDR mechanics=svc-ai-schema-mapper, Activity Log=deep-agent-process-log, Saved Spaces=deep-agent-spaces) and specifies Phase I-A (canonical target schema + UDR control/meta tables) and Phase I-B (ontology-driven RAG foundation seeded from svc-ai-schema-mapper `matchers/`).

Key authoritative rules: canonical hierarchy **Organization→Location→Site→Asset→WorkOrder→Resource→Vendor**; 3 UDR layers (structured/vector/graph); FK = PK-in-another-table + ≥95% referential integrity (else Shared Attribute); vector chunks anchor to **PK only** (Test 1, >1% fail blocks); column overlap ≥30% must be explained by FK (Test 2, ≥1% blocks); shared-attribute-only anchors auto-create a reference table (e.g. asset_make→Manufacturer); UDR script code = `<OrgID><#struct><#unstruct><#tables><#cols><ts>`; keep last 3 versions, never overwrite. Mapping thresholds: deterministic/RAG ≥95% auto, semantic 70–95% suggested, <70% review.

Known doc conflicts to ratify (see plan §1): column auto-merge 95% (PRD) vs 80% (Activity Log sample); custom-space cap 1000 vs 20; WO space 10000 vs 100; assets search 50000 vs 200. Plan recommends the latest-agreed (lower) numbers except auto-merge (keep 95%). [[migration-ui-persistence]] [[project-overview]]
