apiVersion: 1

providers:
  - name: "cafm-platform"
    orgId: 1
    folder: "CAFM Platform"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
